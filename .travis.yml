language: java
jdk:
  - oraclejdk8
  - openjdk8

before_install:
 - chmod +x gradlew

install: ./gradlew setupciworkspace -S
script: ./gradlew build -S

branches:
  only:
  - master

deploy:
  provider: releases
  api_key: "$oauth_key"
  file: SuperheroesAddon-1.1.jar
  skip_cleanup: true